package com.character.races;

public interface Describable {

	/*interface? i can use it on both races and classes by adding a getDescription and adding it to a list/array/toString to print out
	 * a description that combines descriptors from both the race and class.
	 */
	
	String getDescription();
}
